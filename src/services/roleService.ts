import Role from '../models/Role'

class RoleService {
  //   public async getRoles(): Promise<IRole[]> {
  //     return Role.find()
  //   }
  //   public async createRole(name: string, permissions: string[]): Promise<IRole> {
  //     const role = new Role({ name, permissions })
  //     return role.save()
  //   }
  //   public async updateRole(
  //     id: string,
  //     data: Partial<IRole>
  //   ): Promise<IRole | null> {
  //     return Role.findByIdAndUpdate(id, data, { new: true })
  //   }
  //   public async deleteRole(id: string): Promise<IRole | null> {
  //     return Role.findByIdAndDelete(id)
  //   }
}
